{
  "name": "plataforma-tdah",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:back": "npm --prefix Proyecto/backend run dev",
    "dev:front": "npm --prefix Proyecto/frontend run dev",
    "dev": "concurrently -k -n BACK,FRONT -c blue,green \"npm run dev:back\" \"wait-on http-get://127.0.0.1:4000/api/health && npm run dev:front\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^7.2.0"
  }
}
