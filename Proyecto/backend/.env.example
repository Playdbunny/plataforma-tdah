# Ejemplo de archivo .env para el backend
# Renómbralo a ".env" y ajusta las variables según tu configuración local

# Puerto del servidor Express
PORT=4000

# Conexión a tu cluster de MongoDB Atlas
# Formato: mongodb+srv://<usuario>:<password>@<cluster>/<nombre_db>?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://usuario:password@cluster0.xxxxx.mongodb.net/tdah_db

# Configuración de JWT (para autenticación con email/clave y Google)
# Genera una clave aleatoria segura de al menos 32 caracteres
JWT_SECRET=claveSuperSecreta123
JWT_EXPIRES=7d   # Ej: "7d", "12h" o segundos (ej: 3600)

# Configuración para Auth con Google (OAuth2)
GOOGLE_CLIENT_ID=tu_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=tu_google_client_secret
GOOGLE_CALLBACK_URL=http://127.0.0.1:4000/api/auth/google/callback

# Clave de sesión mínima, usada solo para el "handshake" de Google OAuth
# Genera una clave aleatoria segura de al menos 32 caracteres
SESSION_SECRET=sessionSecret123

# URL base del frontend para completar el flujo OAuth
# Cambia este valor en despliegues donde el frontend viva en otro dominio o puerto
FRONTEND_URL=http://127.0.0.1:5173
PUBLIC_BACKEND_ORIGIN=https://synapquest-api.onrender.com

# Configuración de correo para recuperación de contraseña
# EMAIL_FROM define quién aparece como remitente en los correos enviados
EMAIL_FROM=synapquest@gmail.com

# Usa tu cuenta de Gmail (recomendado: crea una contraseña de aplicación)
SMTP_USE_GMAIL=true
SMTP_USER=synapquest@gmail.com
SMTP_PASS=tu_contraseña_de_aplicacion

# Tiempo de validez del token de recuperación (en minutos)
PASSWORD_RESET_TOKEN_MINUTES=60

# Configuración para Google Cloud Storage (GCS)
GCP_PROJECT_ID=tu-project-id-de-gcp
GCS_BUCKET_NAME=nombre-exacto-de-tu-bucket
GOOGLE_APPLICATION_CREDENTIALS=./gcp-key.json

# Origen permitido para CORS (URL del frontend desplegado en Vercel)
CORS_ORIGIN=https://synapquest.vercel.app
